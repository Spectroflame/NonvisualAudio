[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nonvisualaudio"
version = "0.1.0"
description = "Accessible audio analyzer for blind and low-vision audio professionals."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "NonvisualAudio" }]
dependencies = [
    "PySide6>=6.6",
    "numpy>=1.26",
    "scipy>=1.11",
    "soundfile>=0.12",
    "pyloudnorm>=0.1.1",
]

[project.optional-dependencies]
dev = ["pytest>=7", "briefcase>=0.3.17"]

[project.scripts]
nonvisualaudio = "nonvisualaudio.app:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
nonvisualaudio = ["resources/bin/**/*"]
